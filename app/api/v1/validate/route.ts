// /api/v1/validate — canonical path used by the FiveM Lua loader template.
// Delegates entirely to the shared /api/validate handler so logic lives in one place.
export { POST } from '@/app/api/validate/route';
